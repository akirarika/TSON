{
  "name": "@southern-aurora/tson",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "version": "1.0.4",
  "scripts": {
    "build": "bun build ./index.ts --minify --outdir dist --target browser && tsc --declaration --emitDeclarationOnly --outDir dist ./index.ts",
    "p": "npm publish --access public && rm -rf ./dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}